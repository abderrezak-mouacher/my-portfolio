[build]
  command = "npm run build"
  publish = "dist"

[context.production.environment]
  NODE_ENV = "production"
  # Ensure Netlify installs devDependencies so Vite is available during build.
  NPM_CONFIG_PRODUCTION = "false"